# Base URL of the backend API. In local dev, Vite's proxy (vite.config.ts)
# handles /api and /ws so this can stay empty. Set explicitly in production
# builds served from a different origin than the API.
VITE_API_BASE_URL=
